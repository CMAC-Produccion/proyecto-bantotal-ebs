create table SCR005
(
  scrp1 NUMBER(1) not null,
  scrp2 NUMBER(2),
  scrp3 NUMBER(1),
  scrp4 NUMBER(1),
  scrp5 NUMBER(1),
  scrp6 NUMBER(1),
  scrp7 NUMBER(1)
)
;
alter table SCR005
  add primary key (SCRP1);

