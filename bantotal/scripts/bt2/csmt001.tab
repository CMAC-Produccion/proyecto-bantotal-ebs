create table CSMT001
(
  csmt001cod NUMBER(4) not null,
  csmt001des CHAR(40),
  csmt001mod NUMBER(3),
  csmt001top NUMBER(3),
  csmt001rub NUMBER(16),
  csmt001exi CHAR(1),
  csmt001ord NUMBER(2),
  csmt001sbo NUMBER(3),
  csmt001imp NUMBER(3)
)
;
alter table CSMT001
  add primary key (CSMT001COD);

